{
	"name": "kcc",
	"version": "0.0.1",
	"keywords": [],
	"author": "suvanshenoy",
	"description": "tool to convert any toml file to kitty.conf file",
	"scripts": {
		"prod:gen": "bun build --compile kcc.mjs --outfile kcc",
		"local:check": "bun local:fmt && bun local:lint",
		"local:fmt": "biome format --write .",
		"local:lint": "biome lint --write ."
	},
	"devDependencies": {
		"@biomejs/biome": "^2.2.4",
		"@types/node": "^24.3.0"
	},
	"dependencies": {
		"chalk": "^5.6.0",
		"commander": "^14.0.0",
		"toml": "^3.0.0"
	}
}
