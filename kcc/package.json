{
	"name": "kcc",
	"version": "0.0.1",
	"keywords": [],
	"author": "suvanshenoy",
	"description": "tool to convert any toml file to kitty.conf file",
	"scripts": {
		"prod:gen": "bun build --compile ./bin/kcc.mjs --outfile kcc",
		"local:check": "bun local:fmt && bun local:lint",
		"local:fmt": "biome format --write .",
		"local:lint": "biome lint --write ."
	},
	"devDependencies": {
		"@biomejs/biome": "2.1.4",
		"@types/node": "^22.9.0"
	},
	"dependencies": {
		"chalk": "^5.3.0",
		"commander": "^12.1.0",
		"toml": "^3.0.0"
	}
}
